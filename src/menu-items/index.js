// project import
import newMenu from './newMenu';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [newMenu]
};

export default menuItems;
